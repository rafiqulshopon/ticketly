// Jest global setup for the mobile workspace. Add module mocks (e.g.
// jest.mock("@/lib/api")) and react-native-testing-library configuration here as
// the suite grows. The jest-expo preset already mocks native RN modules and the
// platform transform, so a component-rendering test needs little beyond that.
export {};
